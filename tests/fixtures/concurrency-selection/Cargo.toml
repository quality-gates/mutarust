[package]
name = "concurrency-selection-mutator-fixture"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["macros", "rt", "sync"] }

[dev-dependencies]
tokio = { version = "1.52.3", features = ["macros", "rt", "sync"] }
